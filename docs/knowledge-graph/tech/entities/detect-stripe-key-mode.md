# Detect Stripe Key Mode

**Type:** method

A function that takes a raw Stripe key and returns its corresponding StripeKeyMode.

## Neighbors
- [[stripe-configts|Stripe-Config.ts]]
- [[stripekeymode|StripeKeyMode]]
- [[key-mode-pattern|Key Mode Pattern]]

## Appears in
- `apps » web » src » lib » billing » stripe-config.ts`
