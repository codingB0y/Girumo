# OAuth

**Type:** method

A standard protocol for authorization used to delegate access to Google services.<SEP>An authentication callback mechanism that creates a phantom tenant for invited users.

## Appears in
- `apps » web » src » app » api » auth » google » route.ts`
- `auditoria-2026-08-11`
