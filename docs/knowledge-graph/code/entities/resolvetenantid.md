# ResolveTenantId

**Type:** method

A function that retrieves the current tenant identifier based on the authenticated session account.<SEP>A function that retrieves the tenant identifier associated with a specific user ID from the database.<SEP>An asynchronous function that retrieves the current user's tenant ID from the memberships table.<SEP>An asynchronous function responsible for mapping an authenticated user to a valid tenant identifier using database records.

## Neighbors
- [[memberships|Memberships]]
- [[routets|Route.ts]]
- [[memberships-table|Memberships Table]]
- [[supabase-admin|Supabase Admin]]
- [[session|Session]]

## Appears in
- `apps » web » src » app » api » schedules » route.ts`
- `apps » web » src » app » api » media » route.ts`
- `apps » web » src » app » api » links » route.ts`
- `apps » web » src » app » api » subscription » route.ts`
