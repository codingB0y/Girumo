# MessageTemplate

**Type:** concept

A TypeScript type definition representing a message template structure including fields for id, name, category, body, usage count, tenant identification, and creation timestamp.<SEP>Represents a message template with an identifier, name, category, body text, and usage count.

## Neighbors
- [[templatests|Templates.ts]]

## Appears in
- `apps » web » src » lib » stores » templates.ts`
- `apps » web » src » lib » mock-data.ts`
