# CopyInput

**Type:** data

A structured input format containing parameters such as product, type, tone, price, discount, and extra context required for copy generation.<SEP>A structured data type defining inputs required by the copy agent, such as message type, product, tone, price, discount, and extra context.<SEP>An interface representing the input data structure for copy generation including product, price, discount, and type.

## Neighbors
- [[copy-agent|Copy Agent]]
- [[copyvariation|CopyVariation]]

## Appears in
- `apps » web » src » app » api » agents » copy » route.ts`
- `apps » web » src » lib » agents » copy-agent.ts`
