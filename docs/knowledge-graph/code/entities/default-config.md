# Default Config

**Type:** data

The fallback configuration object containing a default reward and goal when no database configuration is found.

## Neighbors
- [[getreferralconfig|GetReferralConfig]]

## Appears in
- `apps » web » src » lib » stores » referrals.ts`
