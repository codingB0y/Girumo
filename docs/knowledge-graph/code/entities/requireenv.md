# RequireEnv

**Type:** method

A helper function that retrieves and validates environment variables, throwing an error if the specified variable is missing.<SEP>A utility function that retrieves and validates required environment variables, throwing an error if a variable is missing.

## Neighbors
- [[supabasets|Supabase.ts]]
- [[stripets|Stripe.ts]]
- [[getstripe|GetStripe]]

## Appears in
- `apps » web » src » lib » supabase » server.ts`
- `apps » web » src » lib » billing » stripe.ts`
