# Dispatch-store.ts

**Type:** artifact

A TypeScript module that manages the dispatching of message campaigns using a JSON-based collection as a queue.

## Neighbors
- [[broadcastsjson|Broadcasts.json]]

## Appears in
- `apps » web » src » lib » dispatch-store.ts`
