# MissionStatusConfig

**Type:** data

Configuration object mapping mission statuses to visual UI properties like label, color, and background.

## Appears in
- `apps » web » src » lib » types » squad-os.ts`
