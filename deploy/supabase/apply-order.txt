infra/migrations/202606240001_base_schema.sql
infra/rls/202606240002_rls_policies.sql
infra/seeds/202606240003_seed_plans.sql
infra/rls/202606240004_storage_policies.sql
infra/migrations/202606240005_engine_rpc.sql
infra/migrations/202606240006_membership_invites.sql
infra/migrations/202607010001_groups_broadcasts_schedules.sql
infra/migrations/202607010010_notifications.sql
infra/migrations/202607010011_tenant_webhooks.sql
apps/web/supabase/migrations/20260701010000_admin_enhancements.sql
apps/web/supabase/migrations/20260701020000_funnel_events.sql
apps/web/supabase/migrations/20260701030000_templates_orders_referrals.sql
apps/web/supabase/migrations/20260701040000_testimonials.sql
apps/web/supabase/migrations/20260701050000_admin_alerts.sql
apps/web/supabase/migrations/20260702120000_flow_pages.sql
apps/web/supabase/migrations/20260715090000_lp_v2.sql
apps/web/supabase/migrations/20260717010000_lp_v2_rollout.sql
apps/web/supabase/migrations/20260723090000_lp_v2_capture_atomicity.sql
# Migração Evolution. Fora de ordem cronológica DE PROPÓSITO: a
# rls_standardization pula tabelas que ainda não existem (to_regclass ... continue),
# então precisa rodar depois de todas as criações para padronizar todas elas.
# Estas quatro estavam aplicadas em produção mas não registradas aqui — um
# ambiente novo nasceria sem o schema da Evolution inteiro.
apps/web/supabase/migrations/20260713100000_rls_standardization.sql
apps/web/supabase/migrations/20260713110000_evolution_provider_columns.sql
apps/web/supabase/migrations/20260713120000_engine_queue_v2.sql
apps/web/supabase/migrations/20260713130000_advisor_cleanup.sql
apps/web/supabase/migrations/20260727120000_leads_and_worker_reads.sql
# F3b: destrava o claim do worker — 'processing' faltava no enum
# engine_event_status. Deve rodar antes de subir o worker (apps/worker).
apps/web/supabase/migrations/20260727130000_engine_event_processing_status.sql
# Features do painel do lojista (PR reland/lojista-panel-features, 28/07):
# meta do mês, e-mail de desconexão e CRUD de automações. Sem estas, as três
# features dão erro em runtime num ambiente novo.
apps/web/supabase/migrations/20260728130000_tenant_settings.sql
apps/web/supabase/migrations/20260728140000_notifications.sql
apps/web/supabase/migrations/20260728145600_automations.sql
# Executor de automações, camada 1 (29/07): estado de execução dos steps.
# Depende de `automations` (acima). Smoke: infra/tests/automation-runs-smoke.sql.
apps/web/supabase/migrations/20260729010000_automation_runs.sql
# F4 (Sprint 5): estado anti-ban por instância (warmup + gate de espaçamento +
# breaker) + claim anti-ban (claim_send_commands) + record_send/failure/prune.
# Fundação do sender do worker; sem ela o claim de envio da F4 não existe.
apps/web/supabase/migrations/20260729120000_engine_antiban_state.sql
# F5 (Sprint 5): ponte disparo → fila do worker. Fan-out (app.enqueue_broadcast),
# roll-up de progresso (reconcile_broadcast_progress) e agendamentos
# (promote_due_schedules). Depende da anti-ban acima (tipos do claim de envio).
# Smoke: infra/tests/dispatch-fanout-smoke.sql.
apps/web/supabase/migrations/20260730100000_dispatch_fanout.sql
# F5 (Sprint 5): aposenta `refresh_status`, tipo de comando que só o engine legado
# drenava — depois do cutover ficaria `queued` para sempre. Cancela as linhas que
# já existem; a rota já não aceita o tipo. Idempotente.
apps/web/supabase/migrations/20260730110000_retire_refresh_status.sql
# Faxina (06/08): remove `release_engine_command`, função órfã de um consumidor de
# `engine_commands` que foi descartado. O anti-ban do claim (F4) tornou o caso dela
# impossível — `claim_send_commands` nunca entrega comando que não pode sair.
# `if exists`: ambiente novo nasce sem ela, então isto é no-op.
apps/web/supabase/migrations/20260806120000_drop_release_engine_command.sql
# F1/PR-1: link mestre da campanha vira linha real em `tracked_links` (antes a UI
# entregava /r/<slug> e nada criava a linha — todo link mestre dava 404). Cria o
# contador atômico de clique (increment_tracked_link_clicks) e faz backfill de uma
# linha por campanha existente. Idempotente. Aplicada em dev e prod em 10/08/2026.
apps/web/supabase/migrations/20260810120000_campaign_master_links.sql
# F1/PR-2: a aba Mensagens passa a disparar via `broadcasts` (pipeline real) em vez
# de `campaign_messages` (tabela sem consumidor). `broadcasts` não tinha `media_name`
# e a agenda exibe o nome do arquivo — coluna opcional para não regredir a tela.
# Aplicada em dev e prod em 10/08/2026.
apps/web/supabase/migrations/20260810130000_broadcasts_media_name.sql
# F1/PR-3: atribuição de cliques passa a ser por ID. Backfill best-effort dos links
# antigos, casando metadata->>'campaignName' com campaign_groups.name dentro do mesmo
# tenant e SÓ quando o nome é único. Aplicada em dev e prod em 10/08/2026.
apps/web/supabase/migrations/20260810140000_backfill_tracked_link_campaign.sql
# Checklist de ativação da Início: duas colunas em `tenant_settings` para o que os
# dados não sabem responder — quando o lojista fechou o card e quando os 5 passos
# ficaram completos. Os passos em si continuam derivados. Idempotente; a tabela já
# tem RLS + policy por tenant. Aplicada em dev e prod em 11/08/2026.
apps/web/supabase/migrations/20260811000000_tenant_settings_onboarding.sql
# Fila do auto-grow. Até aqui a fila só existia em JSON, então em produção — com
# HUBFLOW_USE_SUPABASE ligado — o "grupo lotou, cria o próximo" não tinha onde
# gravar job. Tabela agnóstica de executor de propósito: hoje quem consome é a
# engine Baileys, e o cutover troca só quem faz o claim. Idempotente, RLS ligado.
# Aplicada em dev e prod em 12/08/2026 (19 colunas, 5 índices, RLS + 1 policy nos
# dois; advisor de segurança limpo para a tabela).
apps/web/supabase/migrations/20260812000000_group_grow_jobs.sql

# Clique com data por evento. O contador `tracked_links.clicks` continua sendo a
# fonte dos totais; esta tabela é o histórico que faltava pra responder "quantos
# cliques ontem". Não guarda user-agent, IP nem referer — quem clica é cliente do
# lojista. Idempotente; RLS + policy por tenant.
apps/web/supabase/migrations/20260811100000_link_click_events.sql

# Quadro Scrumban de features no /admin. Dado operacional interno: sem tenant_id,
# RLS ligada e sem policy (deny-all, só service_role). Não entra na publicação
# supabase_realtime — a entrega é por polling. Idempotente.
apps/web/supabase/migrations/20260812120000_board_quadro.sql

# IG Connect — gatilho nativo de Instagram (comentário/DM com palavra-chave -> DM
# com o link rastreado). 3 tabelas com tenant_id, RLS + policy por tenant.
# `ig_accounts.ig_user_id` e `ig_events.source_id` são unique GLOBAIS de propósito:
# o primeiro porque é a chave de resolução de tenant no webhook (a mesma conta em
# dois tenants seria roteamento ambíguo), o segundo porque private reply da Meta é
# 1x por comentário PRA SEMPRE — o unique é o que torna o reenvio da Meta inócuo.
# Nada aqui toca a engine de WhatsApp. Idempotente.
apps/web/supabase/migrations/20260817120000_ig_connect.sql

# Preferências de notificação por evento: opt-out do alerta de desconexão e do
# aviso de disparo falho, ao lado do weekly_report_enabled que já existia.
# Default true (ninguém para de receber por causa da migration). Idempotente.
apps/web/supabase/migrations/20260817120000_notification_preferences.sql

# Super-admin da plataforma por identidade, não por e-mail: cria `platform_admins`
# (PK = auth_user_id) e dropa `platform_settings`, órfã e com 0 linhas nos dois
# bancos desde o refactor de 06/07/2026. A tabela nasce VAZIA e o guard falha
# fechado — semear o primeiro admin por SQL ANTES de deployar o código que a lê.
# Idempotente.
apps/web/supabase/migrations/20260817230000_platform_admins.sql

# Revogacao de sessao (L6 da auditoria de 06/08). O cookie dz_session vale 30
# dias e o logout so apagava o cookie do navegador — o token seguia valido.
# Guarda, por auth_user, o instante a partir do qual os tokens sao recusados:
# revogar e escrever now(), e todo token com iat anterior morre de uma vez.
# RLS ligada com policy deny-all: so o service-role toca. Idempotente.
apps/web/supabase/migrations/20260819200000_session_revocations.sql

# Funcoes do funil das paginas publicas (record_lp_tracking_event e
# confirm_lp_capture). Descobertas em 20/08/2026 existindo SO em producao,
# aplicadas a mao e sem arquivo no repo: em dev o /api/p/track respondia 204 sem
# gravar nada (204 e tambem o codigo do sucesso, entao a falha era invisivel) e
# o /api/p/lead dava 500. Idempotente; em prod e no-op.
apps/web/supabase/migrations/20260820140000_lp_funnel_functions.sql

# notifications: repoe `href` e `read_at`. A tabela tem duas migracoes
# concorrentes com `create table if not exists` e colunas divergentes (a de
# infra/ tem as duas colunas, a de apps/web/ nao tem) — quem aplicou so a
# segunda ficou com a tabela curta. Em dev (21/08/2026) o GET
# /api/notifications dava 500 em todo carregamento do /painel, com
# `42703 column notifications.href does not exist`, e os tres inserts de
# /api/notifications/alerts falhavam sem erro visivel. Idempotente.
apps/web/supabase/migrations/20260821120000_notifications_href_read_at.sql

# plans.price_cents + instances.profile_name: repoe em PROD duas colunas que so
# existiam em DEV. Drift no sentido contrario do usual (dev a frente), entao
# passava local e quebrava so em producao. Em 22/08/2026 `select price_cents
# from plans` e `select profile_name from instances` davam 42703 em prod. Como
# as telas admin descartam o `error` do PostgREST, `data` virava null e a pagina
# renderizava o estado VAZIO sem 500/log: /admin/instancias dizia "Nenhuma
# instancia encontrada" com 6 no banco, e /admin/billing somava MRR sobre um
# planMap vazio. Inclui backfill de preco (197/297/497, confirmado pelo Igor)
# porque preco zerado mente igual, so que sem erro pra culpar. Idempotente.
apps/web/supabase/migrations/20260822120000_plans_price_cents_instances_profile_name.sql

# webhook do Stripe: idempotencia real e guarda de ordem (auditoria 22/08, C.1
# CRITICAL + C.2 HIGH + C.8). Tres objetos: (1) indice unique parcial em
# logs(event, metadata->>'stripe_event_id') para o marcador de idempotencia — o
# handler fazia select-depois-insert sem constraint, entao duas entregas
# concorrentes do mesmo evento processavam as DUAS; resolve tambem o seq scan do
# dedupe em `logs`. (2) subscriptions.stripe_event_created_at. (3) trigger
# subscriptions_reject_stale_event, que descarta escrita mais velha que a ja
# aplicada — sem ele um `customer.subscription.updated` atrasado chegando depois
# de um `deleted` REATIVA assinatura cancelada (reproduzido em dev). Idempotente.
apps/web/supabase/migrations/20260822160000_stripe_webhook_idempotencia.sql

# 22/08/2026 - D.4 da auditoria. RPC public.schema_signature(), consumida pelo
# gate de drift do CI (infra/scripts/check-schema-drift.ts). Existe porque o
# PostgREST nao expoe information_schema, e as alternativas (senha do banco ou
# personal access token da Management API) dariam MAIS acesso do que ler nome
# de coluna. SECURITY DEFINER com search_path fixo, EXECUTE so para
# service_role. Precisa estar nos DOIS bancos, senao o proprio gate acusa a
# ausencia dela como drift.
apps/web/supabase/migrations/20260822180000_schema_signature.sql

# 22/08/2026 - A.2 da auditoria. Zera o privilegio de `anon` em public (tabelas,
# sequencias e funcoes) e instala o event trigger ensure_anon_revoked para
# objeto novo nao nascer aberto. NAO mexe em `authenticated`: 99 policies em 34
# tabelas funcionam por auth.uid()+memberships, e revogar o grant mataria todas.
# Revoga EXECUTE de PUBLIC junto, senao anon executa funcao por heranca. Em dev
# isto tambem fecha o B.4 (drift de GRANT nas RPCs da LP). Idempotente.
apps/web/supabase/migrations/20260822190000_anon_sem_privilegio.sql

# 24/08/2026 - C.5 da auditoria. Da ao tenant um ponteiro proprio para o Customer
# do Stripe (organizations.stripe_customer_id) para o checkout parar de criar um
# Customer novo a cada tentativa abandonada. NAO vai em subscriptions: aquela
# tabela tem unique(tenant_id) e status num enum sem valor neutro, entao linha de
# rascunho ali liberaria os limites do plano pago antes do pagamento. Indice unico
# parcial + backfill do que o webhook ja gravou. Idempotente.
apps/web/supabase/migrations/20260824190000_organizations_stripe_customer_id.sql

# 24/08/2026 - D.5 da auditoria. Agrega o funil do admin no banco
# (funnel_event_counts e funnel_tenant_matrix). As duas devolvem jsonb, e nao
# conjunto de linhas, porque o PostgREST corta conjunto em max-rows (1000) SEM
# erro — era assim que o funil passaria a contar menos sem ninguem ver.
# `create or replace`, idempotente.
apps/web/supabase/migrations/20260824200000_funnel_agregado_no_banco.sql

# 24/08/2026 - A.4 da auditoria. So documentacao: `plans` E catalogo global de
# proposito (linhas de uma organizacao sentinela, validas para todos), entao
# servi-la sem filtro de tenant esta CERTO — filtrar quebraria o checkout das 21
# organizacoes. A constraint plans_code_unique ja existia nos dois bancos; o
# bloco `do $$` so cobre banco novo levantado do zero. Idempotente.
apps/web/supabase/migrations/20260824210000_plans_catalogo_global.sql

# 24/08/2026 - A.3 e o LOW da policy lp_templates_read. So `comment on`: nenhuma
# mudanca de acesso. Escreve que RLS ligado sem policy nestas 4 tabelas de
# infraestrutura e deny-all DELIBERADO (acesso so pelo servidor, que bypassa RLS
# com service-role), e que `using (true)` em landing_page_templates deixou de
# significar leitura anonima quando o PR #138 zerou `anon` em public.
apps/web/supabase/migrations/20260824220000_rls_intencao_explicita.sql

# 25/08/2026 - B.3 da auditoria. Aposenta o Squad OS: 11 tabelas criadas a mao
# so em prod, sem DDL no repo, sem uma linha nova desde a carga inicial de
# 01/07. Junto vao `tenant_webhooks` (0 linhas, nenhum leitor) e a funcao orfa
# `increment_automation_runs`. O conteudo esta guardado em
# docs/arquivo/squad-os/. Com isto a allowlist do gate de drift vai a ZERO e os
# dois bancos ficam com os mesmos 86 objetos.
apps/web/supabase/migrations/20260825140000_aposenta_squad_os.sql

# 26/08/2026 - Aceite dos documentos legais no cadastro. O PR #154 publicou
# /termos e /privacidade, mas nenhuma conta registrava consentimento: o card
# `legal-termos-privacidade` seguia bloqueado por "o signup nao pede aceite".
# Uma linha por documento (terms, privacy) com a versao de LEGAL_VERSION, IP e
# user-agent. Escrita so por service-role; a unica policy deixa o titular ler o
# proprio aceite.
apps/web/supabase/migrations/20260826170000_legal_acceptances.sql

# 28/08/2026 - Modo demonstracao (Fase 3 do paid-first, PR 1). Captura do CTA
# "agendar demonstracao" em /demo: nome + WhatsApp de quem ainda NAO tem conta.
# Sem tenant_id porque e pre-tenant; RLS ligada sem policy, service-role only.
# `step_reached` guarda ate onde a pessoa foi no roteiro — quem desiste no passo
# 2 e quem chega ao pedido sao leads diferentes, e e o campo que liga o demo a
# coorte do PR 2.
apps/web/supabase/migrations/20260828160000_demo_requests.sql

# 29/08/2026 - Saude do numero (R2 da analise competitiva de 28/08). O estado
# anti-ban (instance_send_state, instance_sends, app.instance_daily_cap) nunca
# teve wrapper em `public`, entao o app nao conseguia ler nem o teto do dia: o
# melhor anti-ban da categoria era invisivel para quem paga por ele. A RPC e so
# leitura e chama o MESMO app.instance_daily_cap do claim, para nao criar uma
# segunda verdade sobre o teto que divirja em silencio.
apps/web/supabase/migrations/20260829130000_instance_health.sql
